1. npm init -y
2. npm install --save next react react-dom
3. Next will look for pages folder where are the files for routing
4. The same Next will look for static folder where is the css
5. CSS from static will be applied to all routes
6. Use this file to modify the default behaifior of the server